def collatz_stopping_time(n):
    steps = 0
    while n != 1:
        if n % 2 == 0:
            n = n // 2
        else:
            n = 3 * n + 1
        steps += 1
    return steps

for i in range(1, 1001):
    print(collatz_stopping_time(i))
